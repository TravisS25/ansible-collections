---
argument_specs:
  main:
    short_description: The main entry point for the volume info role.
    options:
      provider:
        type: "str"
        required: true
        choices: "{{ host_provider_list }}"
        description: The provider to gather volume info about